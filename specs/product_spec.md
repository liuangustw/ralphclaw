Implement a utility function called slugify_title(text: str) that:
- lowercases text
- trims leading and trailing whitespace
- converts internal whitespace runs to a single hyphen
- returns a URL-safe simple slug

No new dependencies.
Keep changes minimal.
